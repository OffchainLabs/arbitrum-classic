aliases:
  - &restore_yarn_cache
    keys:
      - yarn-packages-v1-{{ .Branch }}-{{ checksum "yarn.lock" }}
      - yarn-packages-v1-{{ .Branch }}-
      - yarn-packages-v1-
  - &save_yarn_cache
    paths:
      - ~/.cache/yarn
    key: yarn-packages-v1-{{ .Branch }}-{{ checksum "yarn.lock" }}
  - &restore_python_cache
    keys:
      - python-packages-v2-{{ .Branch }}-{{ checksum "packages/arb-compiler-evm/requirements.txt" }}
      - python-packages-v2-{{ .Branch }}-
      - python-packages-v2-
  - &save_python_cache
    paths:
      - ~/.local/lib/python3.7/site-packages
    key: python-packages-v2-{{ .Branch }}-{{ checksum "packages/arb-compiler-evm/requirements.txt" }}
  - &restore_ethbridge_cache
    keys:
      - yarn-packages-v1-{{ .Branch }}-{{ checksum "yarn.lock" }}
      - yarn-packages-v1-{{ .Branch }}-
      - yarn-packages-v1-
  - &save_ethbridge_cache
    paths:
      - ~/.cache/yarn
    key: yarn-packages-v1-{{ .Branch }}-{{ checksum "yarn.lock" }}

  - &restore_go_cache
    keys:
      - go-mod-v2-{{ .Branch }}-{{ checksum "packages/arb-validator/go.sum" }}
      - go-mod-v2-{{ .Branch }}-
      - go-mod-v2-
  - &save_go_cache
    key: go-mod-v2-{{ checksum "packages/arb-validator/go.sum" }}
    paths:
      - ~/go/pkg/mod
  - &restore_conan_cache
    keys:
      - conan-packages-v1-{{ .Branch }}-{{ checksum "packages/arb-avm-cpp/conanfile.txt" }}
      - conan-packages-v1-{{ .Branch }}-
      - conan-packages-v1-
  - &save_conan_cache
    paths:
      - ~/.conan/data
    key: conan-packages-v1-{{ .Branch }}-{{ checksum "packages/arb-avm-cpp/conanfile.txt" }}
version: 2
workflows:
  version: 2
  build:
    jobs:
      - conan-deps
      - yarn-deps
      - python-deps
      - go-deps
      - frontend:
          requires:
            - yarn-deps
            - python-deps
      - ethbridge:
          requires:
            - yarn-deps
      - backend-build:
          requires:
            - go-deps
            - conan-deps
      - backend:
          requires:
            - backend-build
            - ethbridge
jobs:
  yarn-deps:
    docker:
      - image: offchainlabs/build-base:0.1.4
    steps:
      - checkout
      - restore_cache: *restore_yarn_cache
      - run: yarn install --cache-folder ~/.cache/yarn
      - save_cache: *save_yarn_cache
  python-deps:
    docker:
      - image: offchainlabs/build-base:0.1.4
    steps:
      - checkout
      - restore_cache: *restore_python_cache
      - run:
          command: pip3 install -r requirements.txt
          working_directory: /home/user/project/packages/arb-compiler-evm
      - save_cache: *save_python_cache
  conan-deps:
    docker:
      - image: offchainlabs/build-base:0.1.4
    steps:
      - checkout
      - restore_cache: *restore_conan_cache
      - run:
          command: |
            . ../vconan/bin/activate
            mkdir -p packages/arb-avm-cpp/build
            cd packages/arb-avm-cpp/build
            conan install ..
            deactivate
      - save_cache: *save_conan_cache
  go-deps:
    docker:
      - image: offchainlabs/build-base:0.1.4
    steps:
      - checkout
      - restore_cache: *restore_go_cache
      - run:
          command: |
            cd packages/arb-provider-go
            go mod download
      - save_cache: *save_go_cache
  frontend:
    docker:
      - image: offchainlabs/build-base:0.1.4
    environment:
      TEST_RESULTS: /tmp/test-results
    steps:
      - checkout
      - run: mkdir -p $TEST_RESULTS
      - restore_cache: *restore_python_cache
      - restore_cache: *restore_yarn_cache
      - run:
          command: | # use pipenv to install dependencies
            pip3 install --user -r requirements.txt
            python3 setup.py install --user
            yarn install --cache-folder ~/.cache/yarn
          working_directory: /home/user/project/packages/arb-compiler-evm
      - run:
          name: test arb-compiler-evm
          command: |
            coverage run --source=/home/user/project setup.py test
            cd tests/sol-syscall
            truffle migrate --reset --compile-all --network arbitrum
            coverage run --source=/home/user/project truffle_runner.py compiled.json
            cd ../..
            coverage combine .coverage tests/sol-syscall/.coverage
            codecovbash -R /home/user/project -cF arb_compiler_evm
          working_directory: /home/user/project/packages/arb-compiler-evm
      - run:
          name: test arb-provider-ethers
          command: |
            yarn test --coverage --ci --runInBand --reporters=default --reporters=jest-junit
            yarn lint --format junit -o ${TEST_RESULTS}/arb-provider-ethers-lint.xml
            codecovbash -R /home/user/project -cF arb_provider_ethers
          environment:
            JEST_JUNIT_OUTPUT: "${TEST_RESULTS}/arb-provider-ethers.xml"
          working_directory: /home/user/project/packages/arb-provider-ethers
      - run:
          name: test arb-provider-web3
          command: |
            yarn test --pass-with-no-tests --coverage --ci --runInBand --reporters=default --reporters=jest-junit
            yarn lint --format junit -o ${TEST_RESULTS}/arb-provider-web3-lint.xml
            codecovbash -R /home/user/project -cF arb_provider_web3
          environment:
            JEST_JUNIT_OUTPUT: "${TEST_RESULTS}/arb-provider-web3.xml"
          working_directory: /home/user/project/packages/arb-provider-web3
      - run:
          name: test arb-provider-truffle
          command: |
            yarn test  --pass-with-no-tests --coverage --ci --runInBand --reporters=default --reporters=jest-junit
            yarn lint --format junit -o ${TEST_RESULTS}/arb-provider-truffle-lint.xml
            codecovbash -R /home/user/project -cF arb_provider_truffle
          environment:
            JEST_JUNIT_OUTPUT: "${TEST_RESULTS}/arb-provider-truffle.xml"
          working_directory: /home/user/project/packages/arb-provider-truffle
      - store_test_results:
          path: /tmp/test-results
  ethbridge:
    docker:
      - image: offchainlabs/build-base:0.1.4
    steps:
      - checkout
      - restore_cache: *restore_yarn_cache
      - run:
          command: |
            yarn install --cache-folder ~/.cache/yarn
          working_directory: /home/user/project/packages/arb-bridge-eth
      - run:
          name: test arb-bridge-eth
          command: |
            mkdir db
            ganache-cli --db db -p 7545 -m "jar deny prosper gasp flush glass core corn alarm treat leg smart" -q &
            truffle migrate --reset --compile-all
            kill $!
            codecovbash -R /home/user/project -cF arb_bridge_eth
          working_directory: /home/user/project/packages/arb-bridge-eth
      - persist_to_workspace: # store the built files into the workspace for other jobs.
          root: /home/user/project/packages/arb-bridge-eth/
          paths:
            - db
  backend-build:
    docker:
      - image: offchainlabs/build-base:0.1.4
    steps:
      - checkout
      - restore_cache: *restore_go_cache
      - restore_cache: *restore_conan_cache
      - run:
          command: |
            . ../vconan/bin/activate
            mkdir -p packages/arb-avm-cpp/build
            cd packages/arb-avm-cpp/build
            conan install ..
            deactivate
            cmake -DCMAKE_BUILD_TYPE=Debug -DCODE_COVERAGE=True ..
            cmake --build . -j 2
            cp lib/lib* rocksdb/librocksdb.a ../cmachine/
      - persist_to_workspace: # store the built files into the workspace for other jobs.
          root: /home/user/project/packages/arb-avm-cpp/cmachine
          paths:
            - ./*.a
  backend:
    docker:
      - image: offchainlabs/build-base:0.1.4
    environment: # environment variables for the build itself
      TEST_RESULTS: /tmp/test-results # path to where test results will be saved
    steps:
      - checkout
      - run: mkdir -p $TEST_RESULTS
      - attach_workspace:
          at: /home/user/workspace
      - restore_cache: *restore_go_cache
      - restore_cache: *restore_conan_cache
      - run:
          command: |
            cp /home/user/workspace/*.a /home/user/project/packages/arb-avm-cpp/cmachine
            cd packages/arb-validator
            export CGO_LDFLAGS="-lgcov --coverage"
            go install -v ./...
      - run:
          name: test arb-avm-cpp
          command: |
            ./bin/avm_tests -r junit > ${TEST_RESULTS}/arb-avm-cpp.xml
            lcov --capture --directory . --output-file coverage.info
            lcov --remove coverage.info --output-file coverage.info '*/.conan/*' '/usr/*' '*/tests/*' '*/external/*'
            lcov --list coverage.info
            codecovbash -R /home/user/project -f coverage.info -cF arb_avm_cpp
          working_directory: /home/user/project/packages/arb-avm-cpp/build
      - run:
          name: test arb-util
          command: |
            /home/user/go/bin/gotestsum --format short-verbose --junitfile ${TEST_RESULTS}/arb-util.xml -- -race -coverprofile=coverage.txt -covermode=atomic ./...
            codecovbash -R /home/user/project -cF arb_util
          working_directory: /home/user/project/packages/arb-util
      - run:
          name: test arb-avm-go
          command: |
            /home/user/go/bin/gotestsum --format short-verbose --junitfile ${TEST_RESULTS}/arb-avm-go.xml -- -race -coverprofile=coverage.txt -covermode=atomic ./...
            codecovbash -R /home/user/project -cF arb_avm_go
          working_directory: /home/user/project/packages/arb-avm-go
      - run:
          name: test arb-validator
          command: |
            export CGO_LDFLAGS="-lgcov --coverage"
            ganache-cli --db /home/user/workspace/db -p 7545 -b 1 -m "jar deny prosper gasp flush glass core corn alarm treat leg smart" -q &
            /home/user/go/bin/gotestsum --format short-verbose --junitfile ${TEST_RESULTS}/arb-validator.xml -- -race -coverprofile=coverage.txt -covermode=atomic ./...
            codecovbash -R /home/user/project -cF arb_validator
          working_directory: /home/user/project/packages/arb-validator
      - run:
          name: test arb-provider-go
          command: |
            export CGO_LDFLAGS="-lgcov --coverage"
            ganache-cli --db /home/user/workspace/db -p 7545 -b 1 -m "jar deny prosper gasp flush glass core corn alarm treat leg smart" -q &
            /home/user/go/bin/gotestsum --format short-verbose --junitfile ${TEST_RESULTS}/arb-provider-go.xml -- -race -coverprofile=coverage.txt -covermode=atomic ./...
            codecovbash -R /home/user/project -cF arb_provider_go
          working_directory: /home/user/project/packages/arb-provider-go
      - store_test_results:
          path: /tmp/test-results
